//UC1 using if else
{
    const isAbsent = 0;
    let num = Math.floor(Math.random() * 10) % 2;
    if (num == isAbsent) {
        console.log("Employee is Absent");
    }
    else {
        console.log("Employee is Present");
    }
}
